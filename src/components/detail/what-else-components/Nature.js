import React from "react";

import WhatElseLayout from "./WhatElseLayout";

const Nature = ({ tourData }) => {
  return (
    <>
      <WhatElseLayout category="자연" tourData={tourData} />
    </>
  );
};

export default Nature;
